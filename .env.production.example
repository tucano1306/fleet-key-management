# Production Environment Variables (Vercel)
# These should be set in Vercel Dashboard → Settings → Environment Variables

# Database - Vercel Postgres (use POSTGRES_PRISMA_URL)
DATABASE_URL="${POSTGRES_PRISMA_URL}"

# Or external PostgreSQL provider
# DATABASE_URL="postgresql://username:password@host:5432/database?schema=public&pgbouncer=true&connection_limit=1"

# Next.js Authentication
# Generate with: openssl rand -base64 32
NEXTAUTH_SECRET="generate-a-secure-random-string-here"

# Your production URL (Vercel will auto-assign)
NEXTAUTH_URL="https://your-project.vercel.app"
