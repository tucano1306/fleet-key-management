# Environment variables declared in this file are automatically made available to Prisma.
# See the documentation for more detail: https://pris.ly/d/prisma-schema#accessing-environment-variables-from-the-schema

# Prisma supports the native connection string format for PostgreSQL, MySQL, SQLite, SQL Server, MongoDB and CockroachDB.
# See the documentation for all the connection string options: https://pris.ly/d/connection-strings

# Development (Local PostgreSQL)
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/fleet_key_db?schema=public"

# Development (SQLite - Alternative)
# DATABASE_URL="file:./dev.db"

# Production (Vercel Postgres - Use POSTGRES_PRISMA_URL from Vercel)
# DATABASE_URL="postgresql://username:password@host:5432/database?schema=public&pgbouncer=true&connection_limit=1"

# Next.js Secret (generate with: openssl rand -base64 32 or PowerShell command)
NEXTAUTH_SECRET="your-secret-key-change-this-in-production"

# Development
NEXTAUTH_URL="http://localhost:3000"

# Production (Replace with your Vercel URL)
# NEXTAUTH_URL="https://your-project.vercel.app"
